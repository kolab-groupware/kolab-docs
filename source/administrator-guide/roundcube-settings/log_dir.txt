.. _admin_roundcube-settings_log_dir:

``log_dir``
===========

Activate this option if logs should be written to per-user directories.
Data will only be logged if a directry <log_dir>/<username>/ exists and is writable.
