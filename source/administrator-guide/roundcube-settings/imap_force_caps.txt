.. _admin_roundcube-settings_imap_force_caps:

``imap_force_caps``
===================

By default IMAP capabilities are readed after connection to IMAP server
In some cases, e.g. when using IMAP proxy, there's a need to refresh the list after login. Set to True if you've got this case.
